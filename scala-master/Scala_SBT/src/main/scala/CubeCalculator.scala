/**
  *
  */

object CubeCalculator extends App {
  def cube(x: Int) = {
    x * x * x
  }

  def sumar(a: Int, b: Int) = {
    a + b
  }

  def multiplicar(a:Int, b:Int): Unit ={
    a * b
  }

  def restar(a:Int, b:Int): Unit ={

  }
}